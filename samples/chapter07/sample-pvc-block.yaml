apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sample-pvc-block
spec:
  storageClassName: sample-storageclass
  volumeMode: Block
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
